/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { LancamentoResponseDto } from '../../models/lancamento-response-dto';

export interface BuscarPeloCodigo1$Params {
  codigo: number;
}

export function buscarPeloCodigo1(http: HttpClient, rootUrl: string, params: BuscarPeloCodigo1$Params, context?: HttpContext): Observable<StrictHttpResponse<LancamentoResponseDto>> {
  const rb = new RequestBuilder(rootUrl, buscarPeloCodigo1.PATH, 'get');
  if (params) {
    rb.path('codigo', params.codigo, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: '*/*', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<LancamentoResponseDto>;
    })
  );
}

buscarPeloCodigo1.PATH = '/lancamentos/{codigo}';
